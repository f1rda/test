<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firdadev.runsheetv2">

    <!-- Start Adding script barcode scanner -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- End Adding script barcode scanner -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MyApplication"></activity>
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.prarunsheet.PraRunsheetActivity"/>
        <activity android:name=".activity.prarunsheet.praRunsheetNotApprovedActivity" />
        <activity android:name=".activity.runsheet.runsheetNotApprovedActivity" />
        <activity android:name=".activity.view.prarunsheet.DetailScanCnoteActivity" />


    </application>

</manifest>